const log = console.log;

window.onload = () => {
	let windowWidth = window.innerWidth;
	let windowHeight = window.innerHeight;
	info_window_id.innerText = `w: ${windowWidth} \n h: ${windowHeight}`;
};
